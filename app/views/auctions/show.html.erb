<h1><%= @auction.name %></h1>
<ul class="nav nav-pills">
	<li class="active"><a href="#">Info</a></li>
	<li><a href="#">Buyers</a></li>
	<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#">Sellers <span class="caret"></span></a>
			<ul class="dropdown-menu" role="menu">
				<% @auction_types.each do |type|  %>
				<li><a href="#"><%= type != "Misc" ? type.pluralize : type %></a></li>
				<% end %>
			</ul>
	</li>
	<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#">Bids <span class="caret"></span></a>
			<ul class="dropdown-menu" role="menu">
				<% @auction_types.each do |type|  %>
				<li><a href="#"><%= type != "Misc" ? type.pluralize : type %></a></li>
				<% end %>
			</ul>
	</li>
</ul>