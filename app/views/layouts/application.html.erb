<!DOCTYPE html>
<html>
<head>
  <title>Mooauction</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
	<nav class="navbar navbar-inverse" role="navigation">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#auction-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span class="navbar-brand">MooAuction</span>
    </div>
    <% if logged_in? %>
    <div class="collapse navbar-collapse" id="auction-navbar-collapse-1">
     <ul class="nav navbar-nav navbar-left">
        <li><%= link_to 'Auctions', :auctions %></li>
        <li><%= link_to 'Buyers',  '#' %></li>
        <li><%= link_to 'Types', '#' %></li>
        <% if has_admin_role? %>
      <li><%= link_to 'Admin', :admin %></li>
      <% end %>
     </ul>
     <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
            aria-expaned="false"><%= current_user.name %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to 'Change Password', :change_password %></li>
            <li><%= link_to 'Log out', logout_path, method: :delete %></li>
          </ul>
        </li>
     </ul>
    </div>
    <% end %>
  </nav>
  <% if flash.any? %>
  <% flash.each do |name, msg| %>
  <div class="alert alert-<%= name %>" role="alert">
    <%= msg %>
  </div>
  <% end %>
  <% end %>
	<%= yield %>
</div>
</body>
</html>
